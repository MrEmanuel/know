# Config parser

Parse the already validated config.
Resolve globs, extract rules, concepts, links, and other relevant information from the config files.
Store the parsed data in a structured format, in the local SQL database, in tables and as vector embeddings for semantic search.

For tech stack, see techStack.md
